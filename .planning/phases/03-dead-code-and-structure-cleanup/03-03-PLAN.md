---
phase: 03-dead-code-and-structure-cleanup
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src-tauri/src/commands/settings.rs
  - src-tauri/src/commands/images.rs
  - src-tauri/src/commands/mod.rs
files_created:
  - src-tauri/src/core/settings/mod.rs
  - src-tauri/src/core/settings/backup.rs
  - src-tauri/src/core/settings/config.rs
  - src-tauri/src/commands/images/mod.rs
  - src-tauri/src/commands/images/import.rs
  - src-tauri/src/commands/images/export.rs
  - src-tauri/src/commands/images/query.rs
  - src-tauri/src/commands/images/delete.rs
autonomous: true
requirements:
  - CLEAN-03
  - CLEAN-04
user_setup: []
must_haves:
  truths:
    - "settings.rs is under 800 lines and contains only Tauri command handler functions"
    - "images.rs is under 800 lines and contains only Tauri command handler functions"
    - "core/settings/ directory exists with backup.rs and config.rs modules"
    - "commands/images/ directory exists with import.rs, export.rs, query.rs, delete.rs modules"
    - "All pub functions from original settings.rs are re-exported and callable via the same Tauri command paths"
    - "All pub functions from original images.rs are re-exported and callable via the same Tauri command paths"
    - "cargo check passes with zero errors"
    - "cargo clippy --lib -- -D warnings passes"
    - "All existing tests pass"
  artifacts:
    - path: "src-tauri/src/commands/settings.rs"
      provides: "Trimmed to ~800 lines, contains only Tauri command functions (get_config, set_config, get_all_configs, backup_database, backup_database_encrypted, restore_database, restore_database_encrypted, test_lm_studio_connection) plus their direct helper imports"
      max_lines: 800
    - path: "src-tauri/src/core/settings/mod.rs"
      provides: "Module declarations for backup and config submodules"
    - path: "src-tauri/src/core/settings/backup.rs"
      provides: "Backup/restore logic (validate_backup_restore_path, contains_encoded_traversal, is_system_sensitive_directory, normalize_path_for_backup, zip file operations, encryption helpers)"
    - path: "src-tauri/src/core/settings/config.rs"
      provides: "Configuration read/write logic (AppConfig struct, config helpers)"
    - path: "src-tauri/src/commands/images.rs"
      provides: "Trimmed to ~800 lines, contains only Tauri command functions (import_images, get_images, get_image_detail, delete_images, check_broken_links, archive_image, safe_export) plus shared helpers"
      max_lines: 800
    - path: "src-tauri/src/commands/images/mod.rs"
      provides: "Re-exports of all command functions so invoke() resolution works. Also contains shared helpers used across sub-modules (validate_file, sanitize_path, expand_paths, etc.)"
    - path: "src-tauri/src/commands/images/import.rs"
      provides: "Import pipeline logic: file validation, duplicate detection, metadata extraction, DB insertion"
    - path: "src-tauri/src/commands/images/export.rs"
      provides: "Export pipeline logic: safe_export, archive_image, SafeExportResult, path handling"
    - path: "src-tauri/src/commands/images/query.rs"
      provides: "Query/filter/search functions: get_images, get_image_detail, ImageFilters"
    - path: "src-tauri/src/commands/images/delete.rs"
      provides: "Delete/cleanup functions: delete_images, check_broken_links"
  key_links:
    - from: "src-tauri/src/commands/mod.rs"
      to: "core/settings/mod.rs"
      pattern: "pub mod settings|pub use"
    - from: "src-tauri/src/commands/settings.rs"
      to: "core/settings/backup.rs"
      pattern: "use crate::core::settings::"
    - from: "src-tauri/src/commands/mod.rs"
      to: "commands/images/mod.rs"
      pattern: "pub mod images|pub use images::*"
    - from: "src-tauri/src/commands/images/mod.rs"
      to: "commands/images/import.rs"
      pattern: "mod import|pub use import::*"
---

<objective>
Split the two largest files in the codebase — settings.rs (3231 lines) and images.rs (3005 lines) — into focused modules under 800 lines each.

Purpose: Monolithic files harm maintainability, increase merge conflicts, obscure module boundaries, and slow compilation. Each sub-module has a single responsibility: import, export, query, delete, config, or backup.
Output: 8 new files created, 2 existing files trimmed, all Tauri commands still resolve via the same `crate::commands::*` paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
**Pre-requisite:** CLEAN-01 (dead code removal) and CLEAN-05 (missing docs) must have run first. The docs from CLEAN-05 clarify module boundaries and make the split safer.

**Current file state (approximate lines):**
- `commands/settings.rs`: ~3231 lines (before cleanup ~1068 lines before backup/restore logic + ~2000 lines of security/backup/restore helpers + ~436 lines of test code)
- `commands/images.rs`: ~3005 lines (before cleanup ~1100 lines of command handlers + ~900 lines of import logic + ~600 lines of export/archive/shared + ~400 lines of tests)

**settings.rs internal structure (approximate):**
- Lines 1-34: imports + module-level constants
- Lines 35-253: Backup/restore path validation helpers (validate_backup_restore_path, contains_encoded_traversal, is_system_sensitive_directory, normalize_path_for_backup)
- Lines 249-253: AppConfig struct definition
- Lines 255-330: get_config, set_config, get_all_configs commands
- Lines 331-574: backup_database, backup_database_encrypted commands (includes zip/encryption inline logic)
- Lines 576-891: restore_database, restore_database_encrypted commands
- Lines 1066-1092: test_lm_studio_connection command
- Lines 1093-3231: #[cfg(test)] module (~2100 lines of tests)

**images.rs internal structure (approximate):**
- Lines 1-48: imports + constants (MAX_FILE_SIZE, SUPPORTED_EXTENSIONS, SENSITIVE_DIRS)
- Lines 49-270: Path validation helpers (sanitize_path, input_contains_encoded_traversal, normalize_path_strict, validate_import_path, is_sensitive_directory)
- Lines 374-705: Import pipeline helpers (get_available_disk_space, expand_paths, scan_directory, validate_magic_bytes, validate_file, is_duplicate, get_thumbnail_dir, generate_thumbnail_path, update_image_metadata, create_ai_task, insert_image_record)
- Lines 706-1104: import_images command
- Lines 1106-1237: get_images command + ImageFilters struct
- Lines 1239-1373: get_image_detail, delete_images commands
- Lines 1376-1568: check_broken_links, archive_image commands
- Lines 1572-1735: safe_export command
- Lines 1736-3005: #[cfg(test)] module (~1269 lines of tests)

**CRITICAL RULE:** The test modules in both files test private functions. Test code must remain with the implementation it tests. Extract helpers FIRST, then update the command file to `use` the new modules, keeping only command wrappers + thin dispatch.

**Module dependency rules for images/ split:**
- `images/import.rs` depends on: import helpers (validate_file, expand_paths, is_duplicate, etc.), shared helpers (sanitize_path)
- `images/export.rs` depends on: export helpers, safe_export, archive_image, shared helpers
- `images/query.rs` depends on: get_images, get_image_detail, ImageFilters, shared helpers
- `images/delete.rs` depends on: delete_images, check_broken_links, shared helpers
- `images/mod.rs` re-exports all command fns + contains shared helpers used by all sub-modules
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split settings.rs — extract core/settings/backup.rs and core/settings/config.rs</name>
  <files>
    src-tauri/src/core/settings/mod.rs
    src-tauri/src/core/settings/backup.rs
    src-tauri/src/core/settings/config.rs
    src-tauri/src/commands/settings.rs
    src-tauri/src/commands/mod.rs
  </files>

  <read_first>
    Read the full commands/settings.rs file (3231 lines) in two reads: lines 1-250 (imports, constants, path validation helpers), then skim the #[cfg(test)] module starting at line 1093 to understand test structure. Also read commands/mod.rs.
  </read_first>

  <action>
    **Step 1: Create `src-tauri/src/core/settings/mod.rs`**

    ```rust
    //! Settings domain logic — backup/restore and configuration management.
    
    pub mod backup;
    pub mod config;
    ```

    **Step 2: Extract backup/restore logic into `src-tauri/src/core/settings/backup.rs`**

    Move ALL of these functions from `commands/settings.rs` into this new file:
    - `validate_backup_restore_path` (lines 35-159)
    - `contains_encoded_traversal` (lines 162-179)
    - `is_system_sensitive_directory` (lines 182-204, both cfg variants)
    - `normalize_path_for_backup` (lines 208-245)
    
    Add their imports. The functions need:
    - `use std::path::{Component, Path, PathBuf};`
    - `use crate::utils::error::AppError;`
    - `use tracing::{info, warn};`
    
    Make all functions `pub` and `pub(crate)` as appropriate. `validate_backup_restore_path` should be `pub(crate)`.

    Also move ALL zip-file encryption/decryption helper functions used by `backup_database`, `backup_database_encrypted`, `restore_database`, `restore_database_encrypted`. These are inline within those command functions.

    **Step 3: Extract config read/write into `src-tauri/src/core/settings/config.rs`**

    Move the `AppConfig` struct definition (lines 249-253) and any config-related helper functions into this file:
    ```rust
    //! Configuration storage operations.
    
    use serde::{Deserialize, Serialize};
    
    #[derive(Debug, Serialize, Deserialize)]
    pub struct AppConfig {
        pub key: String,
        pub value: String,
    }
    ```

    **Step 4: Trim `commands/settings.rs`**

    After extracting the functions:
    1. Replace the extracted imports and function definitions with `use crate::core::settings::{backup, config};`
    2. Keep ONLY the Tauri command functions:
       - `get_config` (lines 257-283)
       - `set_config` (lines 287-301)
       - `get_all_configs` (lines 305-327)
       - `backup_database` (lines 331-441)
       - `backup_database_encrypted` (lines 443-574)
       - `restore_database` (lines 578-891)
       - `restore_database_encrypted` (lines 893-1064)
       - `test_lm_studio_connection` (lines 1068-1092)
    3. Keep `AppConfig` as a re-export if it's referenced by the command signatures (it is), OR import it from `config`: `use crate::core::settings::config::AppConfig;`
    4. The inline zip/encryption logic within each command stays in the command file (it's tightly coupled to the command flow). Only the pure helper functions moved to `backup.rs`.

    **Step 5: Update `commands/mod.rs`**

    No changes needed — `pub mod settings;` already exists and `commands/settings.rs` still exists.

    **Step 6: Move test code**

    The `#[cfg(test)] mod tests` at line 1093-3231 contains ~2100 lines. Tests for:
    - `validate_backup_restore_path` → move to `core/settings/backup.rs` as `#[cfg(test)] mod tests`
    - Config tests → if any, move to `core/settings/config.rs`
    - Integration tests testing command functions → keep in `commands/settings.rs`

    **After the split**, verify:
    - `commands/settings.rs` is under 800 lines
    - `core/settings/backup.rs` is under 800 lines
    - `core/settings/config.rs` is under 50 lines
    - `cargo check` passes
  </action>

  <verify>
    <automated>
      cd src-tauri && cargo check 2>&1 | tail -10
      echo "---"
      wc -l src-tauri/src/commands/settings.rs
      wc -l src-tauri/src/core/settings/backup.rs
      wc -l src-tauri/src/core/settings/config.rs
    </automated>
  </verify>

  <acceptance_criteria>
    - `wc -l < src-tauri/src/commands/settings.rs` is less than 800
    - `wc -l < src-tauri/src/core/settings/backup.rs` is less than 800
    - `cargo check` passes with zero errors
    - `cargo clippy --lib -- -D warnings` passes
    - `grep -rn "fn validate_backup_restore_path" src-tauri/src/core/settings/backup.rs` matches (function was moved)
    - `grep -rn "pub struct AppConfig" src-tauri/src/core/settings/config.rs` matches (struct was moved)
    - All tests that were moved to backup.rs compile and pass (run with `cargo test --lib core::settings::backup`)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Split images.rs — extract images/import.rs, images/export.rs, images/query.rs, images/delete.rs</name>
  <files>
    src-tauri/src/commands/images/mod.rs
    src-tauri/src/commands/images/import.rs
    src-tauri/src/commands/images/export.rs
    src-tauri/src/commands/images/query.rs
    src-tauri/src/commands/images/delete.rs
    src-tauri/src/commands/images.rs
    src-tauri/src/commands/mod.rs
  </files>

  <read_first>
    Read the full commands/images.rs file (3005 lines) to understand all the helpers. Read commands/mod.rs to see the current module declaration.
  </read_first>

  <action>
    **Step 1: Create `src-tauri/src/commands/images/mod.rs`**

    This module re-exports Tauri command functions and contains shared helpers used across sub-modules.

    ```rust
    //! Image management commands — import, export, query, delete.
    
    pub mod import;
    pub mod export;
    pub mod query;
    pub mod delete;
    
    // Re-export for Tauri command registration
    pub use import::import_images;
    pub use query::{get_images, get_image_detail};
    pub use delete::{delete_images, check_broken_links};
    pub use export::{archive_image, safe_export};
    ```

    Also place **shared helpers** here that are used by multiple sub-modules:
    - `sanitize_path` (lines 65-270) — used by import and export
    - `input_contains_encoded_traversal` (lines 194-215) — used by sanitize_path
    - `normalize_path_strict` (lines 217-268) — used by sanitize_path
    - `is_sensitive_directory` (lines 315-373) — used by sanitize_path
    - `SUPPORTED_EXTENSIONS` constant (lines 17-19)
    - `SUPPORTED_MIME_TYPES` constant (lines 21-32)
    - `MAX_FILE_SIZE` constant (line 14)
    - `MIN_DISK_SPACE_REQUIRED` constant (line 15)
    - `SENSITIVE_DIRS` constant (lines 36-43)
    
    Mark these as `pub(super)` or `pub(crate)` as needed for cross-module access.

    **Step 2: Create `src-tauri/src/commands/images/import.rs`**

    Contains the import pipeline:
    - `get_available_disk_space` (lines 374-442)
    - `expand_paths` (lines 444-474)
    - `scan_directory` (lines 475-499)
    - `validate_magic_bytes` (lines 501-531)
    - `validate_file` (lines 533-633)
    - `is_duplicate` (lines 634-644)
    - `get_thumbnail_dir` (lines 646-656)
    - `generate_thumbnail_path` (lines 658-664)
    - `update_image_metadata` (lines 666-680)
    - `create_ai_task` (lines 682-688)
    - `insert_image_record` (lines 690-705)
    - `PendingImport` struct (lines 708-712)
    - `ImportResult` struct
    - `import_images` command (lines 714-1104)

    Add `use crate::commands::images::*` for shared helpers (sanitize_path, etc.).

    **Step 3: Create `src-tauri/src/commands/images/query.rs`**

    Contains query/search/filter:
    - `ImageFilters` struct (lines 1106-1113)
    - `get_images` command (lines 1116-1237)
    - `get_image_detail` command (lines 1240-)
    
    **Step 4: Create `src-tauri/src/commands/images/delete.rs`**

    Contains delete/cleanup:
    - `delete_images` command (lines 1289-1373)
    - `check_broken_links` command (lines 1378-1445)
    - `BrokenLinkInfo` struct
    - `CheckBrokenLinksResult` struct

    **Step 5: Create `src-tauri/src/commands/images/export.rs`**

    Contains export/archive:
    - `archive_image` command (lines 1449-1568)
    - `safe_export` command (lines 1573-1735)
    - `ArchiveImageResult` struct
    - `SafeExportResult` struct
    - `SafeExportError` struct

    **Step 6: Trim `commands/images.rs`**

    Replace the file with a re-export shim:
    ```rust
    //! Image management commands (split into sub-modules).
    
    pub mod images;
    
    pub use images::*;
    ```
    
    Actually, the cleaner approach: since `commands/mod.rs` has `pub mod images;`, the module must exist at `commands/images.rs` OR `commands/images/mod.rs`. If we create `commands/images/mod.rs`, then `commands/images.rs` must be deleted and `commands/mod.rs` updated to `pub mod images;` pointing to the directory.

    **Better approach — do this:**
    1. Delete `commands/images.rs` entirely
    2. Create `commands/images/mod.rs` with the shared helpers + re-exports
    3. Update `commands/mod.rs` — the `pub mod images;` declaration automatically resolves to `images/mod.rs` directory module when `images.rs` is removed.
    4. Create `commands/images/import.rs`, `export.rs`, `query.rs`, `delete.rs`

    **Step 7: Move test code**

    The `#[cfg(test)] mod tests` in images.rs (lines 1736-3005) contains tests for individual functions. Distribute them to the appropriate sub-module:
    - Import tests → `images/import.rs` as `#[cfg(test)] mod tests`
    - Export tests → `images/export.rs` as `#[cfg(test)] mod tests`
    - Query tests → `images/query.rs` as `#[cfg(test)] mod tests`
    - Delete tests → `images/delete.rs` as `#[cfg(test)] mod tests`
    - Shared helper tests → `images/mod.rs` as `#[cfg(test)] mod tests`

    **After the split**, verify:
    - `images/import.rs`: under 800 lines
    - `images/export.rs`: under 800 lines
    - `images/query.rs`: under 800 lines
    - `images/delete.rs`: under 800 lines
    - `images/mod.rs`: under 400 lines (shared helpers only)
    - ALL function paths resolve from commands/mod.rs
    - `cargo check` passes
  </action>

  <verify>
    <automated>
      cd src-tauri && cargo check 2>&1 | tail -10
      echo "---"
      wc -l src-tauri/src/commands/images/mod.rs
      wc -l src-tauri/src/commands/images/import.rs
      wc -l src-tauri/src/commands/images/export.rs
      wc -l src-tauri/src/commands/images/query.rs
      wc -l src-tauri/src/commands/images/delete.rs
      echo "---"
      echo "File no longer exists check:"
      ls src-tauri/src/commands/images.rs 2>&1 || echo "images.rs deleted as expected"
    </automated>
  </verify>

  <acceptance_criteria>
    - `cargo check` passes with zero errors
    - `cargo clippy --lib -- -D warnings` passes
    - All sub-module files under `commands/images/` are under 800 lines each
    - `commands/images.rs` no longer exists (replaced by `commands/images/mod.rs`)
    - `grep -rn "fn import_images" src-tauri/src/commands/images/import.rs` matches
    - `grep -rn "fn get_images" src-tauri/src/commands/images/query.rs` matches
    - `grep -rn "fn delete_images" src-tauri/src/commands/images/delete.rs` matches
    - `grep -rn "fn safe_export" src-tauri/src/commands/images/export.rs` matches
    - `grep -rn "fn archive_image" src-tauri/src/commands/images/export.rs` matches
    - All previously existing tests pass (`cd src-tauri && cargo test` — note: may have STATUS_ENTRYPOINT_NOT_FOUND issue on Windows, but `cargo test --lib` should work for the refactored code)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| module path resolution | Tauri commands registered in main.rs expect specific module paths; broken resolution causes runtime errors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | Module declarations | mitigate | The key risk is that `#[tauri::command]` functions registered in `main.rs` no longer resolve. After the split, verify that every command function is re-exported at the original path. For settings.rs: commands stay in `commands/settings.rs`, so paths unchanged. For images.rs: re-export from `commands/images/mod.rs` so `crate::commands::images::import_images` etc. still resolve. |
| T-03-05 | Tampering | Test module placement | mitigate | Tests for private functions must stay with the implementation module. Extract tests alongside the functions they test, not into a separate file. |
| T-03-06 | Repudiation | Backward compatibility | accept | No behavior changes. All tests pass. Split is purely structural. |
</threat_model>

<verification>
- [ ] `cd src-tauri && cargo check` passes with zero errors
- [ ] `cd src-tauri && cargo clippy --lib -- -D warnings` passes
- [ ] Each new individual file is under 800 lines
- [ ] `grep -rn '#\[tauri::command\]' src-tauri/src/commands/settings.rs` matches all 8 original commands
- [ ] `grep -rn '#\[tauri::command\]' src-tauri/src/commands/images/ | wc -l` matches the 7 original tauri::command functions
- [ ] No duplicate `pub fn` definitions (grep each command name)
- [ ] `cd src-tauri && cargo test --lib 2>&1 | tail -20` — test module compiles
</verification>

<success_criteria>
1. `commands/settings.rs` is under 800 lines (pure command handlers)
2. `commands/images/` directory has 5 files (mod.rs, import.rs, export.rs, query.rs, delete.rs), each under 800 lines
3. `core/settings/` directory has 3 files (mod.rs, backup.rs, config.rs)
4. All Tauri commands remain accessible via the same `crate::commands::` paths
5. `cargo check` passes with zero errors
6. `cargo clippy --lib -- -D warnings` passes
7. All existing tests compile and pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-dead-code-and-structure-cleanup/03-03-SUMMARY.md`
</output>
